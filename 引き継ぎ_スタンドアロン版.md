# 引き継ぎ — 3院売上集計ダッシュボード（スタンドアロン版 / Node.js）

別PC・別の人（または別のClaude）に引き継ぐための資料。**GAS版とは別物**。これは
Node.js製ツール（追加パッケージなし）で、**2通りの動かし方**がある：
- **ローカル版**：自分のPCで `start.bat` 起動（→第2章）。
- **クラウド公開版**：GitHub + Render で常時公開（→**第2.5章**）。**現在公開中**。

🌐 **公開URL**: https://clinic-sales-dashboard.onrender.com
📦 **GitHubリポジトリ**: https://github.com/tsujikeitojmi/clinic-sales-dashboard

最終更新: 2026-07-17

---

## 0.00. ⭐⭐⭐ 最新（2026-07-17）— 期間集計ページ新設・施術マスタURL化・元フォルダ表示

> **この節0.00が最新。以下すべての節に優先。** デプロイ済み（push 済み＝Render 反映済み）。
> 関連コミット：`d9200fd`（期間集計ページ）／`eee4b15`（/master URL化＋元フォルダ表示）。

### 1) 期間集計ページ `/period`（新規・独立ページ）
- **月別ダッシュボード(index.html)には一切触れていない**（集計コアも無変更）。完全に別ファイル `period.html`。
- サーバー：`getRange(clinicKey, from, to)` を追加（server.js）。任意の日付範囲を会計日 `_day` で絞り、
  `aggregateClinic`/`aggregateItems` に流すだけ。数え方はダッシュボードと同一（会計ベース件数）。
  ルート `GET /period`（ページ配信）と `GET /api/range?clinic&from&to`（データ）を追加。
- **月またぎOK**（範囲が触れる各月のキャッシュを読んで `_day` で結合）。逆順入力は自動で入れ替え。
- 画面：院選択・カレンダー範囲・かんたん選択（今月/先月/過去7日/30日）・サマリーカード・
  **左サイドメニューでカテゴリ絞り込み**・**子カテゴリ別内訳**（親を選ぶと施術の1つ上の階層）・
  **施術内訳（元フォルダ=apiCat列つき）**。各表にコピー。
- **旧形式キャッシュ（`_day`無し）の月は範囲に含められず**「要再取得」を画面で案内（公式集計と同じ事情）。
- 入口：ダッシュボード左下に「📅 期間で集計」リンク。本番では既存Google認証が自動適用。
- 検証済：6月まるごと＝月別ダッシュボードと一致（¥73,924,604/4,403件）、前半+後半＝まるごと（加算性）。

### 2) 施術マスタを `/master` URL化（`#master` 廃止・後方互換あり）
- `#master`（ハッシュ）をやめ、`GET /master`（index.html を配信しフロントが自動でマスタを開く）に変更。
  `openMaster`/`closeMaster`/初期化判定を pathname ベースに。**旧 `#master` リンクも引き続き動く。**
- **施術マスタは別ファイル化していない**（index.html 内オーバーレイのまま）。URLの見え方だけ独立ページ風に。
  ＝軽い方式(A)。完全分離(B=別master.html)はリスク（振り分け=財産に触る）のため見送り。振り分けロジックは無変更。

### 3) 元フォルダ(apiCat)を表示（期間集計・施術マスタの両方）
- medical-force の元フォルダ名（`apiCat`＝院が付けたキャンペーン/分類フォルダ）を、施術の「1つ上の階層」として表示。
  - 期間集計の施術内訳：「元フォルダ（API）」列。
  - 施術マスタの各行：施術名の下に元フォルダ名（絵文字なし・ツールチップで全文）。
- 集計・振り分けには影響しない表示専用（振り分けキーは optionId のまま。[[project-clinic-dashboard]]）。

### 補足：ローカルも本番と同じ Supabase を共有（サンドボックスではない）
- `.env` に SUPABASE_URL/KEY があれば、**ローカル起動でも本番と同じ共有マスタを読み書き**する。
  ローカルでの振り分け変更・自動振り分けは Supabase 経由で**本番にも反映**される。練習用ではない点に注意。
- 自動振り分け（`runAutoAssignBg`）はページを開くと走るが**★未分類のみ対象**。手動振り分け済みは動かない。

---

## 0.0. ⭐⭐ （2026-07-16）— 数え方を「件数」に統一 ＋ Google認証

> **下の節0/節0.5/節3/節4 と食い違ったら、この節0.0が正（さらに上の節0.00が最優先）。**
> デプロイ済み（main に push 済み＝Render 反映済み）。関連コミット：
> `26e754c`（件数の戻し）／`2ec9a8f`（件数へ全面統一）／`3be6c68`（Google認証）。

### 1) 数え方は「件数（会計ベース）」に統一。「延べ人数」はダッシュボードから廃止
- **理由（本人確定）**：同じ人が別の日に同じ施術を受けたら **2回として数えたい**（＝回数）。
  延べ人数（ユニーク患者数）は再来を1に潰すので意味が合わない。「会計ごとに数えるのが正しい」。
- **件数の定義** = 会計ごとにカウント。コース契約と売上0は除外、同一会計内で同じ施術は1件。
  `aggregateClinic` の `byCat.count` と `aggregateItems` の `count` は**同じ数え方**（合計が一致する）。
- **件数に統一した画面**：カード下段4枚 / サイドバー各行（○件）/ カテゴリ別明細 / ランキング（件数TOP）/
  施術内訳パネル / 月別サマリー / 日別サマリー。**すべて総件数に揃う**（3院×3か月で検証済み）。
- **削除した関数**：`pplByCatMap` / `getTreePpl`（index.html）。**延べ人数を再導入しないこと。**

### 2) 例外：公式集計テーブル（medical-force一致）だけは「人数＝ユニーク患者数」のまま
- 節0で作った公式集計テーブル（`aggregateByKind` の `ninzuu`）は**公式画面と一致させるため据え置き**。
  ここだけ「人数」表記が残るのは正常。**触らない。**
- 注意：公式の「人数」は**足せない**（施術2,535＋物品470＋その他99 に意味なし）。合計行は「—」表示。
  行ごとにユニーク患者を数えるので、複数区分を使った人が重複するため。

### 3) 数が3つとも違うのは仕様（バグではない）— 心斎橋 2026/06 の例
- **総件数 4,403**（明細を会計ごとに数える。契約・¥0除外）
- **公式 個数 4,505**（数量ベース。消化は別列232、契約・¥0も含む）
- **公式 人数 2,535**（施術を受けた実患者数）
- **売上はどれも ¥73,924,604 で一致**（唯一の共通の錨）。件数と個数の差102は「数量>1(+241)/契約(+17)/¥0(+56)/消化を別列(−212)/その他kind除外(−1)/会計内重複(+1)」で埋まる。

### 4) Google ログイン認証（本番のみ有効）
- `GOOGLE_CLIENT_ID` と `ALLOWED_EMAILS` の**両方が env にある時だけ**認証ON（`AUTH_ON`）。
- **本番(Render)は設定済み＝ログイン画面が出るのが正常**。ローカルは未設定＝認証OFFで素通り。
- 別PCでローカル起動時にログインを出したいなら、`.env` にこの2つを足す。外部ライブラリなし（Google Identity）。

### 5) 既知の未解決（軽微・未着手）
- **月別/日別サマリーの「総金額」列**が、カード/グラフのツールチップと約¥195万ズレる（心斎橋2026/06）。
  表は「通常+CP+媒体」＝★未分類・除外を**含まない**、カードは**含む**全件合計のため。
  同じ行で件数だけ全件・金額だけ除外という不整合だが、**金額は確定済みで変更禁止のため未修正**（要相談）。
- `git stash@{0}`：「月別グラフ 1年/3年切替＋2023年遡り」試作が退避したまま。**stashは別PCに来ない**（必要なら別途push）。

---

## 0. ⭐ アップデート（2026-07-01）— medical-force 公式画面と完全一致　※人数の扱いは節0.0が上書き

> **この節が最新。以下の古い節と食い違う場合はこの節が正。** デプロイ済みコミット `6ee869f`（main に push 済み＝Render 反映済み）。

### 何をやったか（すべて実データ＝公式画面のスクショと照合して確認）
medical-force の公式集計画面（売上分析：項目＝施術/薬剤/物品/その他、列＝個数/消化回数/人数/売上）と
**数字を1円・1人まで一致**させた。照合済み：**心斎橋 2025/08・2025/11・2026/05・2026/06／福岡 2026/03／新宿 2026/03**。

### 1) 売上ロジック（集計コア `aggregateClinic`）— ※古い節4の記述は無効
- **消化計上**：コース契約(`courseContractAmountWithTax>0`)は計上しない。**消化した時**に計上（`courseDigestionAmountWithTax`）、単品は `genuinePriceWithTax`。
- **丸めは切り捨て(round_down)** に統一（`Math.floor`）。medical-force の `decimal_processing_type:"round_down"` と一致。
  端数の出る物品なども**1円まで公式一致**（以前は四捨五入で数円ズレていた）。
- ※「消化除外・契約日計上・四捨五入」と書いてある**下の節4は古い**。現行は上記。

### 2) 公式集計テーブル（新機能 `aggregateByKind`）
`/api/dashboard` の戻り値に `official:{rows, enriched}` を追加。画面カード下に表を表示（`renderOfficial`）。
kind（`施術/薬剤/物品/その他`）別に：
- **個数** = 消化でない明細の**数量(`quantity`)合計**（単発＋コース契約）
- **消化回数** = 消化(`courseDigestion>0`)明細の**数量合計**
- **人数** = その kind の明細を持つ**ユニーク患者数(`visitorId`)**。ただし価格(`genuinePriceWithTax`)のある行のみ（セット構成品の未価格行は除外）。その他行は kind 空欄行を除外。
- **売上** = 上記の消化計上（金額と同一）
- 「その他」行は 前受金・返金・調整の寄せ集め。**個数/消化は0**にしている。

### 3) 人数＝ユニーク患者数（`visitorId`）　⚠️ **この項目は節0.0で撤回済み（2026-07-16）**
> 当時は施術内訳を「人数」に変えたが、**後日すべて「件数（会計ベース）」に戻して統一した**。
> 公式集計テーブル（`aggregateByKind`）の人数だけが `visitorId` ベースで残る。以下は経緯として残置。
- ~~施術内訳テーブルの「件数」→「人数」に変更（`aggregateItems`）~~ → **件数に戻した**。
  - `aggregateItems` の `count` は今は会計ベース件数（`aggregateClinic` と同じ数え方）。
- カードの「来院数」＝会計数（`countVisits`）は独自定義で健在。

### 4) キャッシュに項目追加（`slimValues`）→ 旧月は「要再取得」
- 保存項目に **`kind` / `quantity` / `visitorId`** を追加（公式集計・人数に必要）。
- **本番/既存キャッシュ(Supabase)の古い月にはこれが無い** → 公式集計表は「⚠️要再取得」表示（`enriched=false`）。
  各月で **🔄最新取得**（今月）／**📥過去分取得**（過去月）を押すと入り、公式一致する。**売上は再取得不要**（表示時計算）。
- 上の「照合済み」の月は再取得済み＝Supabaseに入っているのでそのまま出る。

### 5) 今月は裏で自動再取得（`autoRefreshBg`）
- 今月を開くと、まずキャッシュ即表示→**裏でAPI再取得して差し替え**（ボタン不要）。
- 対象は**今月だけ**（過去月は変わらないので対象外）。**直近10分以内に取得済みならスキップ**。
- 取得ボタン（🔄最新取得 / 📥過去分取得）は**保険として両方残す**方針。

### 既知の許容差（バグではない）
- **新宿 2026/03 の施術のみ**：個数↔消化回数の**内訳が28ズレ**（個数+28/消化−28、合計・人数・売上は一致）。
  API(daily-accounts)上 `digest=0` で返るが公式内部では消化扱いの明細があり、**API項目からは再現不可**。他院月は内訳も一致。→ **許容**。

### コードの主な場所（server.js）
`aggregateClinic`（集計コア/売上）・`aggregateByKind`（公式集計）・`aggregateItems`（施術内訳=人数）・
`countVisits`（来院数）・`slimValues`/`isEnriched`（キャッシュ項目）。画面(index.html)：`renderOfficial`・`autoRefreshBg`。

### まだ本番に無い作業（このPCローカルのみ・未push）
- 「**月別グラフ 1年/3年 切替＋2023年1月まで遡り**」を試作 → `git stash`（`stash@{0}`）に退避したまま。
  **別PCには来ない**（stashはローカル）。必要なら別途 push が要る。（2026-07-16時点でも未着手）

---

## 0.5 追加セッション（2026-07-02）— 振り分けUX刷新＋施術マスタページ　※push済み（2026-07-16確認）

> ✅ **この節0.5の変更は push 済み＝本番反映済み**（2026-07-16時点）。以下は内容の記録。

### 1) ハイコックス薬剤を分離（ポテンツァ混入＝二重計上の修正）
- 原因：`ジュベルック/リジュラン/スネコス/エクソソーム/ACRS` が CATEGORY_TREE で
  **ポテンツァ>CP-25 と ハイコックス の両方に同名で存在** → ハイコックスの薬剤がポテンツァ集計に混入。
- 対応：ハイコックス側を専用カテゴリ **`〇〇（ハイコックス）`** にリネーム（server.js `CATEGORY_TREE`/`DEFAULT_CATEGORIES`）。
- 既存データ：**`migrateHicox()`（起動時・冪等）** で、名前に「コックス」を含む該当 **134件** を付け替え。**Supabase反映済み**。
- **表示は短縮**：`displayCat()`（index.html）で画面上は「（ハイコックス）」を隠す（親の階層で分かるため）。
  **データ/保存/集計は生の名前のまま**（混ざらない）。カテゴリ名を出す全20箇所に適用。

### 2) 振り分けカード：親→子ドリルダウン
- 親カテゴリを選ぶ→その場で子カテゴリ表示→確定（`renderPendingChoices`/`pickGroup`/`assignCat`/`drillBack`）。

### 3) おすすめ強化（全カテゴリ共通）
- `suggestBestPath()`：施術名から**ルート→葉のおすすめパス**を推定。**先祖の一致も加点**するのでハイコックス系を正しく推す。
- 親カードのおすすめ＋**子にもおすすめを最上表示**。`getPending` が `suggestPath` を返す。
- 別名辞書 `CATEGORY_ALIAS` に **FatX**（脂肪溶解注射：FatX/fatX/Fat X…）やハイコックス薬剤の別名を追加。

### 4) 施術マスタ 管理ページ（独立ページ `#master`）
- 左下「🗂 施術マスタを編集」 or URL `…/#master`。全画面ページ（モーダルでない）。
- **院ごと**：`GET /api/master?clinic=` がその院のキャッシュ全月の全施術＋現在の振り分けを返す（未分類含む・金額なし）。
- 2ペイン：左ナビ（★未分類＋カテゴリ別件数）／右一覧（子カテゴリ小分け・行ごとに **種別トグル＋カテゴリ変更ドリルダウン**）。検索・種別フィルタ。
- 保存は `/api/assign`（振り分けカード・施術内訳編集と同一）＝**集計に即反映**。
  実証：脱毛→HIFUに1件移すと集計が移動、戻すと復元。閉じると自動再集計。

### コードの主な追加場所
- server.js：`CATEGORY_TREE`（（ハイコックス）分離）・`migrateHicox`・`NODE_INFO`/`ownScore`/`suggestBestPath`・`getMasterList`（`/api/master`）。
- index.html：`displayCat`・施術マスタ一式（`openMaster`/`renderMaster`/`renderMasterNav`/`renderMasterMain`/`openCatPick`/`assignMasterCat`…）・カードのドリルダウン。

---

## 1. これは何か / ゴール

医療法人の3院（**心斎橋・新宿・福岡**。将来 名古屋も）の売上を medical-force API から取得し、
**施術カテゴリ別に集計してブラウザにダッシュボード表示**するツール。

- ブラウザUI：左に院アコーディオン（中に施術カテゴリ）、サマリーカード4枚、
  月別グラフ（棒+折れ線・過去12か月）、ランキング、カテゴリ別明細、施術内訳。
- 未分類の施術を**カード式で1件ずつ振り分け**（かんたんなものは自動）。
- 明るくクリーンな美容クリニック風デザイン（ローズゴールド基調）。

---

## 2. 動かし方（新しいPCでの手順）

1. **Node.js をインストール**（https://nodejs.org/ja の LTS、18以上。確認は `node -v`）。
2. このフォルダ（`施術別api/test` 一式）を新PCにコピー。
   - **`data/` フォルダも必ず一緒にコピー**（後述。振り分け結果＝財産）。
   - **`.env` もコピー**（API認証情報。無ければ新規作成）。
3. `start.bat` をダブルクリック → ブラウザで `http://localhost:7700` が開く。
   - 初回起動で `.env` が無ければ自動作成してメモ帳が開くので、認証情報を記入。

### 重要：SSL証明書の対処（社内ネット等）
この環境では `fetch failed (UNABLE_TO_VERIFY_LEAF_SIGNATURE)` が出たため、
**Node を `--use-system-ca` 付きで起動**して解決済み。`start.bat` は
`node --use-system-ca server.js` で起動する。手動起動時も同様に：
```
node --use-system-ca server.js
```
新PCでこのエラーが出なければ `--use-system-ca` は無くても可（付けても害なし）。

### .env（Script Propertiesの代わり。コードに直書きしない）
```
CLINIC1_CLIENT_ID / CLINIC1_CLIENT_SECRET / CLINIC1_CLINIC_ID   (心斎橋)
CLINIC2_... (新宿)
CLINIC3_... (福岡)
SUPABASE_URL / SUPABASE_SERVICE_KEY        # 全PC共有データ（無ければローカルJSONのみで動く）
# 任意: PORT=7700
# 任意: GOOGLE_CLIENT_ID / ALLOWED_EMAILS  # 両方あるとGoogleログイン認証ON（節0.0-4）。ローカルは通常不要
```
`=` の右に値を貼るだけ。1院だけでも動く（他院は選ぶとエラーになるだけ）。
**`.env`（特に client_secret）は他人に渡さない・ネットに上げない。**

---

## 2.5 クラウド公開版（GitHub + Render）← 現在の本番

ローカルを使わず、ブラウザだけで誰でも開ける常時公開版。**現在これで運用中**。

### 構成
- **GitHub**（`tsujikeitojmi/clinic-sales-dashboard`）にソースを置く。
- **Render**（無料プラン / Singaporeリージョン）が GitHub と連携して自動デプロイ。
- **`main` ブランチに push すると、Renderが自動で再ビルド＆再公開**する。
- 設定は `render.yaml`（Blueprint）に記載。手動でWeb Serviceを作る場合は
  Build=`npm install` / Start=`node server.js` / Node / Free。

### 環境変数（秘密情報）はどこにあるか
- **`.env` はGitHubに上げていない**（`.gitignore`済み）。
- 代わりに **Renderダッシュボードの「Environment」タブ**に11個を登録済み：
  `SUPABASE_URL` / `SUPABASE_SERVICE_KEY` / `CLINICx_CLIENT_ID` /
  `CLINICx_CLIENT_SECRET` / `CLINICx_CLINIC_ID`（x=1,2,3）。
- 値を変える/キー再発行したら、**Renderの画面で更新→自動再デプロイ**。

### 別PCから引き継いで更新する手順
1. **Git** と **Node.js(18+)** を入れる。
2. リポジトリをクローン：
   ```
   git clone https://github.com/tsujikeitojmi/clinic-sales-dashboard.git
   cd clinic-sales-dashboard
   ```
3. ローカルで動かすなら `.env` を自分で用意（値はRenderの画面か安全な共有先から）。
   `npm install` → `node server.js` で `http://localhost:7700`。
4. コードを直したら反映：
   ```
   git add -A
   git commit -m "変更内容"
   git push
   ```
   → push後、Renderが自動で再デプロイ（数分でURLに反映）。
5. デプロイ状況・エラーは **Render → 該当サービス → Logs** で確認。

### 無料プランの注意（重要）
- **スリープ**：15分アクセスが無いとサーバー停止 → 次回アクセスで起動に
  **30秒〜1分**かかる（「読み込み中…」が長くても故障ではない）。
- **`data/` はサーバー上に無い**（`.gitignore`済み）。データの正は **Supabase**。
  起動直後にキャッシュが無い場合は **`🔄 最新取得`** でAPIから取り直す。
- 無料プランは月の稼働時間に上限あり。常時アクセスが多いなら有料(Starter $7/月)も検討。

### GitHubへ push できない/落ちた時
- まず壊れていないか確認：`git status`（cleanならOK）。
- リモートに届いたか：`git ls-remote origin`（出力があれば反映済み）。
- 途中で落ちても基本リポジトリは壊れない。`git push` をやり直すだけでよい。
- `git reset --hard` や `.git/*.lock` の削除は、状態を確認してから。

---

## 3. ファイル構成

| ファイル | 役割 |
|---|---|
| `server.js` | 本体。APIから取得→集計→ローカルサーバ配信。依存パッケージなし（Node標準のみ）。 |
| `index.html` | 月別ダッシュボード＋施術マスタ（`/master`）。`server.js`が配信。 |
| `period.html` | **期間集計ページ（`/period`）。独立ファイル**（2026-07-17新設・節0.00）。 |
| `start.bat` | 起動用（Nodeチェック→.env用意→ブラウザ起動→`node --use-system-ca server.js`）。 |
| `package.json` | npm/Render用。`start`=`node server.js`、Node18+指定。依存パッケージなし。 |
| `render.yaml` | **Renderデプロイ設定（Blueprint）**。サービス名/リージョン/起動コマンド/環境変数キー。 |
| `.env` / `.env.example` | API認証情報。`.example`をコピーして`.env`を作る。**GitHubには上げない**（Render側に登録）。 |
| `README_セットアップ.md` | セットアップ手順。 |
| `data/master.json` | **施術マスタ** = optionId→{category,type}。振り分け結果のローカルバックアップ（正はSupabase `mfdash_master`、2026-07時点 約6900件）。 |
| `data/categories.json` | カテゴリ（グループ）一覧のローカルバックアップ（正はSupabase `mfdash_categories`、2026-07時点 76件）。振り分けで増える。 |
| `data/cache/CLINICx_YYYY_M.json` | 月次の**API生データ**キャッシュ（`{fetchedAt, values}`）。 |
| `Code.gs` / `GASダッシュボード_引き継ぎ.md` | **旧GAS版の残骸。このツールでは未使用**（消してもOK）。 |

### `data/` の中身は財産
- `master.json` … 人＋自動で振り分けた結果。**これを失うと振り分けやり直し**。
- `cache/` … 取得済み月のデータ。あると表示が一瞬。
- 新PCへは**この`data/`ごとコピー**すれば、振り分けも取得済みデータも引き継げる。

---

## 4. 集計ロジック（※現行は「節0」が正。以下は旧記述で一部無効）

> ⚠️ **この節の「消化除外・契約日計上・四捨五入」は古い。** 現行は **消化計上＋切り捨て(round_down)**。
> 正確な現行仕様は **「節0」** を参照。以下は経緯として残す。

GAS版の引き継ぎ資料で確定した集計コアをそのまま移植（`server.js` の `aggregateClinic`）。
**心斎橋5月でMF公式画面と照合済み。物品・返金・薬剤はほぼ完全一致、施術の差は方針差（契約日計上 vs 消化計上）でバグなし。**

各 `paymentItems[]` に対して：
```
contract = courseContractAmountWithTax
digest   = courseDigestionAmountWithTax
genuine  = genuinePriceWithTax

if contract<=0 and digest>0:  → 集計対象外（コース消化は除外）
sales = round(contract>0 ? contract : genuine)
if sales==0: スキップ
```
- **按分しない**（genuineは割引適用後の実額）。
- **件数 = 同じ会計 × 同じ(カテゴリ,種別) = 1件**（同会計内で重複排除）。
- `daily-accounts` は **1日ずつ取得**（epoch範囲を広げると「31日ちょうど」で
  `Unexpected error` が出ることがあるため）。並列16本で取得（後述）。

### カテゴリ名寄せ
- APIの `category` は院が付けたフォルダ名でキャンペーン名や日付混じりで揺れる→集計軸に使えない。
- 安定キーの **optionId** をマスタ（`master.json`）で **集計カテゴリ＋種別** に名寄せ。
- マスタに無い optionId は `★未分類` に集約 → 振り分けUIで人＋自動が割り当てる。
- `category==='除外'` にすると集計から外れる。

### 種別（通常 / CP / 媒体）
- 媒体KW: カンナム/キレイパス/ホットペッパー/HPB/トリビュー/くまポン
- CP KW: キャンペーン/ゲリラ/フェア/感謝祭/スキンチケット/CP
- それ以外: 通常

#### 新宿・福岡はタグで種別を機械的に決める（起動時マイグレーション・冪等）
院スタッフが medical-force 側に付けるタグを唯一の基準にする。手動で種別を変えても次回起動でタグ基準に戻る。
判定の優先順位はどちらも **媒体名 > 「通常」の文字 > 無印＝CP**（「通常」は括弧不問）。
種別だけを変更し、カテゴリ（施術の振り分け）は一切変更しない。物販／除外／★未分類は対象外。

| | 新宿 `migrateShinjukuFolderTypes` | 福岡 `migrateFukuokaTagTypes` |
|---|---|---|
| 媒体の判定 | **施術名 or フォルダ名**（最新月） | 施術名＋全月フォルダ名＋apiCat |
| 通常/CPの判定 | フォルダ名のみ（最新月） | 同上（連結テキスト） |
| 対象 | 新宿キャッシュに実在する optionId のみ | 福岡キャッシュに実在する optionId のみ |

- **2026/08/14 追加**：新宿は媒体名をフォルダではなく**施術名**に付け替えた施術が出てきたため、
  媒体だけは施術名も見るようにした（通常/CP の判定は従来どおりフォルダ名のみ）。
- **落とし穴**：medical-force は過去月を再取得しても「**現在の**」施術名・フォルダ名を返す。
  タグを付け替えたら **全月を `refresh=1` で再取得 → サーバー再起動** で全月に反映される。
  キャッシュが古いままだと変更前の名前で判定されるので拾えない。

**再取得手順**（新宿の例。福岡も院キーを変えるだけで同じ）
```bash
node server.js                    # AUTH_ON=false ならローカルは認証不要
# 各月（1月あたり約25秒）
curl "http://localhost:7700/api/dashboard?clinic=CLINIC2&year=2026&month=8&refresh=1"
# 全月ぶん回したらサーバー再起動 → 起動時マイグレーションが全月のタグで再判定
```

---

## 5. 振り分け（重要な仕様）

### 保存単位は optionId だけ（月・院に紐づかない）
`master.json` は optionId をキーに保存。集計時に月・院は見ない。結果：
- **同じ院・別の月 → 共有される**（再振り分け不要。新メニューのoptionIdだけ追加で必要）。
  - 例：心斎橋 5月と6月で343個のoptionIdが共通、6月の新規は109個だけ。
- **別の院 → 共有されない**（medical-forceのoptionIdは院ごとに別物。心斎橋6月∩新宿6月=0）。
  院ごとに振り分けが要るが、多くは下記「自動振り分け」で片付く。

### 振り分けの対象範囲（スコープ）— 過去を遡らなくてよい
振り分けUI上部のトグルで2モード（既定は**全期間**）：
- **全期間（取得済み全月）**＝`scope=all`。**キャッシュ済みの全月をまたいで**未分類を
  optionIdで合算し、1パスでまとめて振り分け。過去月にしか出ないメニューも拾える。
- **この月だけ**＝`scope=month`。選択中の月の未分類のみ。
- 注意：全期間＝**キャッシュ済みの月**が対象。`📥 過去分を取得`で過去月を取得しておくと網羅的。
- 仕組み上、一度振り分けたoptionIdは全月に効くので、全期間モードで一度やれば基本は完了。

### かんたん自動振り分け（`autoAssign`）
振り分けUIを開くと、まず **「明確に1カテゴリだけに一致するもの」を自動で振り分け**、
残った曖昧なものだけ人が確認する。条件（`autoPickCategory`）：
- カテゴリ別名（`CATEGORY_ALIAS`）にヒットしたカテゴリが **ちょうど1つ**、かつ
- 一致語が3文字以上 or カテゴリ名そのものが施術名に含まれる。
- 複数カテゴリ該当（例「ハイコックス・スキンボトックス」「A×B×C」の複合メニュー）や
  一致なしは**人へ**。
- 実測：心斎橋 全期間14か月で **579件自動 / 769件を人へ**（この月だけだと168件中の一部）。

### おすすめ並べ替え（`rankCategories`）
振り分けカードの候補は **近い順に並べ替え**、最上位に「おすすめ」バッジ。
別名辞書 `CATEGORY_ALIAS`（server.js内）で表記揺れを吸収（例 ハイフ/ウルトラフォーマー→HIFU、
水光/スネコス→肌育注射、ステラ/M22→フォトフェイシャル、ボツ→ボトックス）。
**実データで「この施術はこのカテゴリに来てほしい」が出たら、ここに語を足すと精度UP。**

### 施術ごとの編集
「施術内訳」表の各行「編集」ボタン → カテゴリ/種別をその場で変更・保存（誤り修正用）。

---

## 6. パフォーマンス（重さ対策）

- **日次取得を並列16本**（`FETCH_CONCURRENCY`）。初回1か月 約18秒。
- 取得した**生データを `data/cache` に保存**（集計後ではなく生データ＝振り分け変更後も再集計で反映）。
- **当月もキャッシュ優先**で即表示（約0.3秒）。**`🔄 最新取得`** を押した時だけAPI再取得。
- **`📥 過去分を取得`**：選択院の過去12か月のうち未取得月だけ順に取得（キャッシュ済みは即スキップ）。
- 月別グラフは**キャッシュにある月だけ**値が出る（無い月は0）。

---

## 7. サーバAPI（`index.html`から fetch）

| メソッド/パス | 用途 |
|---|---|
| `GET /api/config` | 院一覧（色付）/カテゴリ一覧/既定年月 |
| `GET /api/dashboard?clinic&year&month&refresh=1` | ダッシュボード本体。refresh=1でAPI再取得。 |
| `GET /api/range?clinic&from&to` | 期間集計（`/period`用）。任意範囲を`_day`で絞り集計。月またぎ可（2026-07-17・節0.00）。 |
| `GET /api/overview?year&month` | 全院のサイドバー用（キャッシュのみ・API叩かない） |
| `GET /api/pending?clinic&year&month&scope` | 未分類の候補（おすすめ・近い順）。`scope=all`でキャッシュ全月合算、`month`で当月。 |
| `POST /api/assign` `{assignments:[{optionId,name,apiCat,category,type}]}` | マスタへ保存（新カテゴリは categories.json へ自動追加） |
| `POST /api/auto-assign` `{clinic,year,month,scope}` | かんたん自動振り分け（`scope=all`/`month`） |

ポート **7700**（5173はViteの既定でファビコンキャッシュが残るため変更した経緯あり）。

---

## 8. デザイン

- ライトテーマ。基調 `--bg:#f7f5f4` / アクセント **ローズゴールド `--accent:#b76e79`** / サブ **セージ `--accent2:#7fa99b`**。
- グラフ：通常=ブルーグレー / CP=ゴールド / 媒体=ローズ / 件数(線)=セージ。
- 院ドット色：心斎橋=ローズ / 新宿=ゴールド / 福岡=セージ（`server.js` CLINIC_LIST）。
- ファビコン：ローズ地に白い「D」モノグラム＋金ドット（`index.html`の`<link rel="icon">`に
  SVGをbase64で埋め込み。外部取得なし）。

---

## 9. 第1版で割り切った点 / TODO（次の人へ）

- **原価率ランキング**は未実装（原価データがAPI/マスタに無い）。マスタに原価列を足せば対応可。
  現状ランキングは「売上(粗利)TOP / 件数TOP」の2軸。
- サマリーカードの「前回実施」は定義未確定 → 暫定で**平均単価**を表示。
- 月別グラフの過去月は**キャッシュ依存**（一括取得は `📥 過去分を取得` で院ごと）。
  3院まとめて一括取得するボタンは未実装（要望あれば追加可）。
- 別名辞書 `CATEGORY_ALIAS` / 自動振り分けの閾値は実データで育てる前提。
  未登録例：「BTX→ボトックス」「リニアQ→？」など、出てきたら追記。
- 院をまたいだ振り分けの引き継ぎ（施術名ベースで候補提示）は未実装。
- 認証トークンは取得のたびに発行（有効期限1日）。キャッシュはしていない。

---

## 10. よくある操作

- 起動：`start.bat` ダブルクリック（または `node --use-system-ca server.js`）。停止：黒い窓で `Ctrl+C`。
- 院切替：左サイドの院名クリック。カテゴリ絞り込み：カテゴリ名クリック（再クリックで解除）。
- 振り分け：左下「未分類を振り分け」→ 上部トグルで**全期間/この月**を選択 → 自動振り分け後、残りをカードで確認。
- 最新化：`🔄 最新取得`（選択月）/ `📥 過去分を取得`（過去12か月）。
- 振り分けの修正：最下部「施術内訳」の各行「編集」。
